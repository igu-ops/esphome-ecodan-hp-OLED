i2c:
  sda: 38
  scl: 39
  scan: true
  sda_pullup_enabled: true
  scl_pullup_enabled: true
  frequency: 40000  # slower I2C for stability

font:
  - file: "gfonts://Open Sans"
    id: font_small
    size: 12

display:
  - platform: ssd1306_i2c
    model: "SH1106 128x64"
    address: 0x3C
    lambda: |-
      it.clear();
      it.print(0, 0, id(font_small), "HELLO");
